# joint_limits.yaml allows the dynamics properties specified in the URDF to be overwritten or augmented as needed

# For beginners, we downscale velocity and acceleration limits.
# You can always specify higher scaling factors (<= 1.0) in your motion requests.  # Increase the values below to 1.0 to always move at maximum speed.
/**:
  ros__parameters:
    default_velocity_scaling_factor: 0.05
    default_acceleration_scaling_factor: 0.05

    # Specific joint properties can be changed with the keys [max_position, min_position, max_velocity, max_acceleration]
    # Joint limits can be turned off with [has_velocity_limits, has_acceleration_limits]
    joint_limits:
      joint1:
        has_velocity_limits: true
        max_velocity: 0.5
        has_acceleration_limits: true
        max_acceleration: 0.05
        has_position_limits: true
        min_position: -3.1416    # 硬件限位
        max_position: 3.1416     # 硬件限位
        # 添加安全裕度
        soft_lower_limit: -3.0  # 软件下限，比硬件多5°裕度
        soft_upper_limit: 3.0   # 软件上限，比硬件多5°裕度
        # 接近限位时的警告阈值
        k_position: 0.1        # 用于限位检测的系数
      joint2:
        has_velocity_limits: true
        max_velocity: 0.5
        has_acceleration_limits: true
        max_acceleration: 0.05
        has_position_limits: true
        min_position: -3.1416    # 硬件限位
        max_position: 3.1416     # 硬件限位
        # 添加安全裕度
        soft_lower_limit: -3.0  # 软件下限，比硬件多5°裕度
        soft_upper_limit: 3.0   # 软件上限，比硬件多5°裕度
        # 接近限位时的警告阈值
        k_position: 0.1        # 用于限位检测的系数
      joint3:
        has_velocity_limits: true
        max_velocity: 0.5
        has_acceleration_limits: true
        max_acceleration: 0.05
        has_position_limits: true
        min_position: -3.1416    # 硬件限位
        max_position: 3.1416     # 硬件限位
        # 添加安全裕度
        soft_lower_limit: -3.0  # 软件下限，比硬件多5°裕度
        soft_upper_limit: 3.0   # 软件上限，比硬件多5°裕度
        # 接近限位时的警告阈值
        k_position: 0.1        # 用于限位检测的系数
      joint4:
        has_velocity_limits: true
        max_velocity: 0.5
        has_acceleration_limits: true
        max_acceleration: 0.05
        has_position_limits: true
        min_position: -3.1416    # 硬件限位
        max_position: 3.1416     # 硬件限位
        # 添加安全裕度
        soft_lower_limit: -3.0  # 软件下限，比硬件多5°裕度
        soft_upper_limit: 3.0   # 软件上限，比硬件多5°裕度
        # 接近限位时的警告阈值
        k_position: 0.1        # 用于限位检测的系数
      joint5:
        has_velocity_limits: true
        max_velocity: 0.5
        has_acceleration_limits: true
        max_acceleration: 0.05
        has_position_limits: true
        min_position: -3.1416    # 硬件限位
        max_position: 3.1416     # 硬件限位
        # 添加安全裕度
        soft_lower_limit: -3.0  # 软件下限，比硬件多5°裕度
        soft_upper_limit: 3.0   # 软件上限，比硬件多5°裕度
        # 接近限位时的警告阈值
        k_position: 0.1        # 用于限位检测的系数
      joint6:
        has_velocity_limits: true
        max_velocity: 0.5
        has_acceleration_limits: true
        max_acceleration: 0.05
        has_position_limits: true
        min_position: -3.1416    # 硬件限位
        max_position: 3.1416     # 硬件限位
        # 添加安全裕度
        soft_lower_limit: -3.0  # 软件下限，比硬件多5°裕度
        soft_upper_limit: 3.0   # 软件上限，比硬件多5°裕度
        # 接近限位时的警告阈值
        k_position: 0.1        # 用于限位检测的系数